# File: pricing.html & styles.css
Update the existing `pricing.html` and `styles.css` to reflect the following changes:

1. **Remove the Free Trial “plan card.”** Only include the three paid plans: Starter, Pro, Agency.

2. **Buttons:**  
   - On all plan cards, replace the “Select Plan” button text with **“Try for Free”**.  
   - Buttons link to `signup.html?plan=starter|pro|agency` accordingly.

3. **Feature Headings for Pro & Agency:**  
   - In the **Pro** card, above its feature list, insert a subheading:  
     ```html
     <p class="plan-extra" data-lang="proExtra">Everything in Starter, plus:</p>
     <!-- sr: Sve iz Starter paketa, plus: -->
     ```  
   - In the **Agency** card, above its feature list, insert:  
     ```html
     <p class="plan-extra" data-lang="agencyExtra">Everything in Pro, plus:</p>
     <!-- sr: Sve iz Pro paketa, plus: -->
     ```

4. **Billing Toggle Redesign:**  
   Replace the existing toggle markup & styles with a pill-style toggle matching this design:  
   - A rounded background track in `var(--light)`, width ~120px, height ~40px.  
   - A circular thumb in `var(--primary)` that slides left (Monthly) or right (Annually).  
   - Labels “Monthly” and “Annually (10% off)” inside the toggle track.  
   - Active label text in white, inactive in `var(--dark)`.  
   - Use CSS transitions for the thumb slide.  
   - Include minimal JS stub to toggle an `annual` class on the price wrapper.

5. **Responsive Layout:**  
   - Three cards in a flex row on ≥1024px, two per row on 641–1023px, and single column on ≤640px.  
   - Ensure the toggle and cards center-align on mobile, with proper spacing.

6. **CSS Variables & Styling:**  
   - Continue using:
     ```css
     :root {
       --primary: #2e8b57;
       --accent: #f4b400;
       --dark: #222;
       --light: #f9fafb;
     }
     ```
   - Add styles for `.plan-extra`: slightly smaller italic text in `var(--dark)`.

7. **Accessibility & i18n:**  
   - All user-facing text remains in `data-lang` with Serbian comments.  
   - Toggle control should have `role="switch"` and `aria-checked` attributes.

8. **Footer & Nav:**  
   - Preserve existing header/nav and footer; no changes needed there.

Generate the updated `pricing.html` and `styles.css` files in full. Ensure the toggle matches the attached visual reference (pill with sliding thumb) and that the plan cards reflect the new headings and button text.