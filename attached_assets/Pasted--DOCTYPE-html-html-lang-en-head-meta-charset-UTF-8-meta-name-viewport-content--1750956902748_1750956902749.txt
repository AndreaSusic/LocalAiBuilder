<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mobile Dashboard Wireframe with Tabs</title>
  <style>
    /* Reset & base */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: sans-serif;
      background: #d3d3d3;
      color: #2a2a2a;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }

    /* Sticky top bar */
    .mobile-header {
      position: sticky;
      top: 0;
      background: #d3d3d3;
      border-bottom: 1px solid #2a2a2a;
      padding: 8px 12px;
      display: flex;
      align-items: center;
      gap: 8px;
      z-index: 10;
    }
    .mobile-header button {
      background: white;
      border: 1px solid #2a2a2a;
      border-radius: 6px;
      padding: 6px;
      font-size: 1rem;
      cursor: pointer;
    }
    .mobile-header .hamburger { font-size: 1.5em; padding: 4px 8px; }
    .mobile-header .icon-btn { font-size: 1.2em; padding: 6px 8px; }
    .mobile-header .small-btn { padding: 6px 10px; font-size: 0.9rem; }

    /* Scrollable content */
    .container {
      flex: 1;
      overflow-y: auto;
      padding: 16px;
    }

    /* Panel styles */
    .panel {
      background: white;
      border: 1px solid #2a2a2a;
      border-radius: 10px;
      margin-bottom: 16px;
      padding: 12px;
    }
    .panel h2 {
      font-size: 1rem;
      margin-bottom: 8px;
    }

    /* Versions list */
    .panel.versions ul {
      list-style: none;
    }
    .panel.versions li {
      background: #f9f9f9;
      border: 1px solid #ccc;
      border-radius: 6px;
      margin-bottom: 8px;
      padding: 8px;
      font-size: 0.9rem;
    }

    /* Chat/Editor Tabs */
    .tabs {
      display: flex;
      border-bottom: 1px solid #ccc;
      margin-bottom: 12px;
    }
    .tabs button {
      flex: 1;
      background: #eee;
      border: 1px solid #ccc;
      border-bottom: none;
      padding: 8px;
      font-size: 0.9rem;
      cursor: pointer;
    }
    .tabs button.active {
      background: white;
      border-top: 2px solid #2a2a2a;
      font-weight: bold;
    }

    .tab-content > div {
      display: none;
    }
    .tab-content > div.active {
      display: block;
    }

    /* Chat textarea */
    .tab-content .chat textarea {
      width: 100%;
      height: 100px;
      border: 1px solid #ccc;
      border-radius: 6px;
      padding: 8px;
      font-size: 0.9rem;
      resize: none;
      margin-bottom: 8px;
    }
    .tab-content .chat button {
      background: white;
      border: 1px solid #2a2a2a;
      border-radius: 6px;
      padding: 8px 12px;
      font-size: 0.9rem;
      cursor: pointer;
      width: 100%;
    }

    /* Editor placeholder */
    .tab-content .editor {
      font-size: 0.9rem;
      line-height: 1.4;
    }
  </style>
</head>
<body>

  <!-- Top bar -->
  <header class="mobile-header">
    <button class="hamburger">☰</button>
    <button class="icon-btn">🔔</button>
    <button class="small-btn">Publish</button>
    <button class="small-btn">View Preview</button>
  </header>

  <!-- Scrollable main content -->
  <div class="container">
    <!-- Versions Panel -->
    <div class="panel versions">
      <h2>Versions</h2>
      <ul>
        <li>Version 1</li>
        <li>Version 2</li>
        <li>Version 3</li>
      </ul>
    </div>

    <!-- Chat / Editor Panel with Tabs -->
    <div class="panel">
      <h2>Chat / Editor</h2>
      <div class="tabs">
        <button id="tab-chat" class="active">Chat</button>
        <button id="tab-editor">Editor</button>
      </div>
      <div class="tab-content">
        <div class="chat active">
          <textarea placeholder="Type your message…"></textarea>
          <button>Send ➤</button>
        </div>
        <div class="editor">
          <!-- replace with your editor controls -->
          <p><strong>Editor Controls</strong></p>
          <p>– Text formatting<br>
             – Image & Video tools<br>
             – Component list…</p>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Tab switching logic
    const chatTab = document.getElementById('tab-chat');
    const editorTab = document.getElementById('tab-editor');
    const chatContent = document.querySelector('.tab-content .chat');
    const editorContent = document.querySelector('.tab-content .editor');

    chatTab.addEventListener('click', () => {
      chatTab.classList.add('active');
      editorTab.classList.remove('active');
      chatContent.classList.add('active');
      editorContent.classList.remove('active');
    });
    editorTab.addEventListener('click', () => {
      editorTab.classList.add('active');
      chatTab.classList.remove('active');
      editorContent.classList.add('active');
      chatContent.classList.remove('active');
    });
  </script>

</body>
</html>